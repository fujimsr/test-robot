name: オウム返しロボット
on:
  workflow_dispatch:
    inputs:
      todays_mood:
        description: '今の気分を入力してください'
        required: true
        default: '絶好調'

jobs:
  echo-job:
    runs-on: ubuntu-latest
    steps:
      - name: 実行
        run: echo "藤森さんの今の気分は... ${{ inputs.todays_mood }} です！"
